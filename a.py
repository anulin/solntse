def al(j,k,A,B):
    for i in A[k]:
        if i==j:
            return(j)
        B=
        if k==al(k,i,A,B):
        return(None)
    else return(al())
s=[int(i) for i in input().split()]
A=[[] for n in range(s[0])]
B=[0 for n in range(s[0])]
print(B)
for j in range(s[1]):
    c=input().split()
    A[int(c[0])].append(int(c[1]))
print()